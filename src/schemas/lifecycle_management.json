{
  "$id": "https://meta.comcast.com/firebolt/lifecycle_management",
  "title": "LifecycleManagement",
  "oneOf": [
    {
      "$ref": "#/definitions/State"
    },
    {
      "$ref": "#/definitions/CloseReason"
    }
  ],
  "definitions": {
    "State": {
      "type": "string",
      "description": "The application lifecycle state",
      "enum": [
          "initializing",
          "inactive",
          "foreground",
          "background",
          "unloading",
          "suspended"
      ]
    },
    "CloseReason": {
        "type": "string",
        "description": "The application close reason",
        "enum": [
            "remoteButton",
            "userExit",
            "error"
        ]
    }
  }
}
